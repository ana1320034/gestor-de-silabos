// App.jsx — punto de entrada limpio.
// El render real está en main.jsx (BrowserRouter + rutas protegidas).
// Este archivo se conserva vacío para compatibilidad con el grafo de módulos de Vite.
export default function App() {
  return null;
}
